kind: Kustomization

namespace: matrix
resources:
- synapse.yaml
- element.yaml
- httproute.yaml

configMapGenerator:
- name: synapse-data
  files:
  - homeserver.yaml
  - matrix.cloud.buc.sh.log.config
- name: element-config
  files:
  - element-config.json
