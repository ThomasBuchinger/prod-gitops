kind: Kustomization

namespace: matrix
resources:
- synapse.yaml
- element.yaml

configMapGenerator:
- name: synapse-data
  files:
  - homeserver.yaml
  - chat.cloud.buc.sh.log.config
- name: element-config
  files:
  - element-config.json
