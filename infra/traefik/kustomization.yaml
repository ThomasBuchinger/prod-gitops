namespace: traefik

helmCharts:
- name: traefik
  releaseName: traefik
  namespace: traefik
  includeCRDs: true
  repo: https://traefik.github.io/charts
  version: 40.2.0
  valuesFile: values.yaml

resources:
- ns.yaml